rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /inventory/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /finance/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
