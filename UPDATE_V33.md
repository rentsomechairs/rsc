# Rental UX v33

- Removed the leftover `initGoogleDeliveryAutocomplete()` startup call.
- Automatic delivery calculation remains disabled.
- Delivery continues to collect the address and displays: “A delivery fee will be discussed.”
- Cache version bumped to `rental-ux-v33`.
